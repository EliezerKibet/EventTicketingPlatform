name: Event Ticketing Platform Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '9.0.x'
        
    - name: Restore dependencies
      run: dotnet restore EventTicketing.Tests/
      
    - name: Build Test Project
      run: dotnet build EventTicketing.Tests/ --no-restore
      
    - name: Run Tests
      run: dotnet test EventTicketing.Tests/ --verbosity normal --logger trx --results-directory "TestResults"
      
    - name: Publish Test Results
      uses: EnricoMi/publish-unit-test-result-action@v2
      if: always()
      with:
        files: TestResults/**/*.trx
